# === Rust ===
/target/                # Build output
**/*.rs.bk              # Rust backup files
Cargo.lock              # Only for libraries (keep it for binaries)

/# === VS Code ===
.vscode/                # Editor settings (optional – keep if team-shared)
*.code-workspace

# === OS and editor junk ===
.DS_Store               # macOS
Thumbs.db               # Windows
*.swp                   # Vim swap files
*.swo

# === Logs and misc ===
*.log
*.tmp
